# Remote templates

Folders here are **seeds** for `bun run pull-remote` when a remotes.json entry
uses `"repo": "template:<folder-name>"`.

Example: `template:example-promotions` → `templates/example-promotions`.

For real teams, replace `template:…` with a private git URL after the remote
lives in its own repository.
